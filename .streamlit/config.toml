[theme]
primaryColor        = "#4A7C59"   # Deep sage — replaces Streamlit's default red everywhere
backgroundColor     = "#F7F3EE"   # Warm parchment
secondaryBackgroundColor = "#EFF4EC"  # Sage-tinted sidebar
textColor           = "#2C2016"   # Warm brown, never pure black
font                = "sans serif"
